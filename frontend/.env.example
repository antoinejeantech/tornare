# Backend API base URL used by Vite
VITE_API_URL=http://localhost:8000

# Explicit signup UI override: true/false. If omitted, defaults to enabled in dev and disabled in prod.
VITE_PUBLIC_SIGNUP_ENABLED=false
