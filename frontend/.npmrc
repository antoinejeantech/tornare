# vite-plugin-pwa@1.2.0 does not yet list vite@8 in its peer dep range
# (peerDependencies: "vite": "^3.1.0 || ^4 || ^5 || ^6 || ^7").
# Remove this once vite-plugin-pwa ships support for vite@8.
legacy-peer-deps=true
